{
  "buildCommand": "pnpm --filter @workspace/api-spec run codegen && pnpm --filter @workspace/license-admin run build && cp -r artifacts/license-admin/dist/public ./public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://dmh-license-api.onrender.com/api/:path*"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
